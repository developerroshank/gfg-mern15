// 6. Given the array of strings ['apple', 'banana', 'grape'], return the length of each string.

const str = ['apple', 'banana', 'grape'];

const strLen = str.map(s => s.length);
console.log(strLen);